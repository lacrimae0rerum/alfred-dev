---
name: next
description: "Alfred Dev command: decide and execute the next operational step from current project state. Invoke as $alfred-dev:next."
---

# $alfred-dev:next

Treat this skill as the Codex-native wrapper for `$alfred-dev:next`.

1. Treat the text after `$alfred-dev:next` as `$ARGUMENTS`.
2. Read `../../commands/next.md` completely before acting.
3. Follow that command contract exactly, adapting only the user-facing invocation name to `$alfred-dev:next` when explaining how to continue.
