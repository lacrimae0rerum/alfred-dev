---
name: verify
description: "Alfred Dev command: prepare or close human validation/UAT separate from automated tests. Invoke as $alfred-dev:verify."
---

# $alfred-dev:verify

Treat this skill as the Codex-native wrapper for `$alfred-dev:verify`.

1. Treat the text after `$alfred-dev:verify` as `$ARGUMENTS`.
2. Read `../../commands/verify.md` completely before acting.
3. Follow that command contract exactly, adapting only the user-facing invocation name to `$alfred-dev:verify` when explaining how to continue.
