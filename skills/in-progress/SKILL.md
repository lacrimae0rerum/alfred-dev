---
name: in-progress
description: "Alfred Dev command: list only tasks that are currently in progress. Invoke as $alfred-dev:in-progress."
---

# $alfred-dev:in-progress

Treat this skill as the Codex-native wrapper for `$alfred-dev:in-progress`.

1. Treat the text after `$alfred-dev:in-progress` as `$ARGUMENTS`.
2. Read `../../commands/in-progress.md` completely before acting.
3. Follow that command contract exactly, adapting only the user-facing invocation name to `$alfred-dev:in-progress` when explaining how to continue.
