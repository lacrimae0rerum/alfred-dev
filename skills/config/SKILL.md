---
name: config
description: "Alfred Dev command: configure autonomy, stack, compliance, optional agents, personality, and memory. Invoke as $alfred-dev:config."
---

# $alfred-dev:config

Treat this skill as the Codex-native wrapper for `$alfred-dev:config`.

1. Treat the text after `$alfred-dev:config` as `$ARGUMENTS`.
2. Read `../../commands/config.md` completely before acting.
3. Follow that command contract exactly, adapting only the user-facing invocation name to `$alfred-dev:config` when explaining how to continue.
