---
name: progress
description: "Alfred Dev command: summarize kanban, blockers, traceability, UAT, and operational project state. Invoke as $alfred-dev:progress."
---

# $alfred-dev:progress

Treat this skill as the Codex-native wrapper for `$alfred-dev:progress`.

1. Treat the text after `$alfred-dev:progress` as `$ARGUMENTS`.
2. Read `../../commands/progress.md` completely before acting.
3. Follow that command contract exactly, adapting only the user-facing invocation name to `$alfred-dev:progress` when explaining how to continue.
