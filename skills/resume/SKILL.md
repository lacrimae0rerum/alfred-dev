---
name: resume
description: "Alfred Dev command: resume an active session or pending handoff. Invoke as $alfred-dev:resume."
---

# $alfred-dev:resume

Treat this skill as the Codex-native wrapper for `$alfred-dev:resume`.

1. Treat the text after `$alfred-dev:resume` as `$ARGUMENTS`.
2. Read `../../commands/resume.md` completely before acting.
3. Follow that command contract exactly, adapting only the user-facing invocation name to `$alfred-dev:resume` when explaining how to continue.
