---
name: discuss
description: "Alfred Dev command: refine an idea or phase and persist discovery before opening a delivery flow. Invoke as $alfred-dev:discuss."
---

# $alfred-dev:discuss

Treat this skill as the Codex-native wrapper for `$alfred-dev:discuss`.

1. Treat the text after `$alfred-dev:discuss` as `$ARGUMENTS`.
2. Read `../../commands/discuss.md` completely before acting.
3. Follow that command contract exactly, adapting only the user-facing invocation name to `$alfred-dev:discuss` when explaining how to continue.
