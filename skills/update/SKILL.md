---
name: update
description: "Alfred Dev command: check for and apply Alfred Dev plugin updates. Invoke as $alfred-dev:update."
---

# $alfred-dev:update

Treat this skill as the Codex-native wrapper for `$alfred-dev:update`.

1. Treat the text after `$alfred-dev:update` as `$ARGUMENTS`.
2. Read `../../commands/update.md` completely before acting.
3. Follow that command contract exactly, adapting only the user-facing invocation name to `$alfred-dev:update` when explaining how to continue.
