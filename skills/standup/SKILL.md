---
name: standup
description: "Alfred Dev command: concise SonIA standup with current work, blockers, progress, and next step. Invoke as $alfred-dev:standup."
---

# $alfred-dev:standup

Treat this skill as the Codex-native wrapper for `$alfred-dev:standup`.

1. Treat the text after `$alfred-dev:standup` as `$ARGUMENTS`.
2. Read `../../commands/standup.md` completely before acting.
3. Follow that command contract exactly, adapting only the user-facing invocation name to `$alfred-dev:standup` when explaining how to continue.
