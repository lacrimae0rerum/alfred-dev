---
name: validate
description: "Alfred Dev command: validate operational integrity of kanban, traceability, UAT, and local sync. Invoke as $alfred-dev:validate."
---

# $alfred-dev:validate

Treat this skill as the Codex-native wrapper for `$alfred-dev:validate`.

1. Treat the text after `$alfred-dev:validate` as `$ARGUMENTS`.
2. Read `../../commands/validate.md` completely before acting.
3. Follow that command contract exactly, adapting only the user-facing invocation name to `$alfred-dev:validate` when explaining how to continue.
