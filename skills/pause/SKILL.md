---
name: pause
description: "Alfred Dev command: pause the current work and generate a persistent handoff. Invoke as $alfred-dev:pause."
---

# $alfred-dev:pause

Treat this skill as the Codex-native wrapper for `$alfred-dev:pause`.

1. Treat the text after `$alfred-dev:pause` as `$ARGUMENTS`.
2. Read `../../commands/pause.md` completely before acting.
3. Follow that command contract exactly, adapting only the user-facing invocation name to `$alfred-dev:pause` when explaining how to continue.
