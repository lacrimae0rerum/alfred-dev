---
name: status
description: "Alfred Dev command: show current phase, completed phases, pending gate, and active agent. Invoke as $alfred-dev:status."
---

# $alfred-dev:status

Treat this skill as the Codex-native wrapper for `$alfred-dev:status`.

1. Treat the text after `$alfred-dev:status` as `$ARGUMENTS`.
2. Read `../../commands/status.md` completely before acting.
3. Follow that command contract exactly, adapting only the user-facing invocation name to `$alfred-dev:status` when explaining how to continue.
