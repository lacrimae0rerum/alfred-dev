---
name: fix
description: "Alfred Dev command: bugfix flow with diagnosis, TDD correction, regression validation, and security review. Invoke as $alfred-dev:fix."
---

# $alfred-dev:fix

Treat this skill as the Codex-native wrapper for `$alfred-dev:fix`.

1. Treat the text after `$alfred-dev:fix` as `$ARGUMENTS`.
2. Read `../../commands/fix.md` completely before acting.
3. Follow that command contract exactly, adapting only the user-facing invocation name to `$alfred-dev:fix` when explaining how to continue.
