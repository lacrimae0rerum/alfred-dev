---
name: map-codebase
description: "Alfred Dev command: map an existing repository and persist codebase context before implementation. Invoke as $alfred-dev:map-codebase."
---

# $alfred-dev:map-codebase

Treat this skill as the Codex-native wrapper for `$alfred-dev:map-codebase`.

1. Treat the text after `$alfred-dev:map-codebase` as `$ARGUMENTS`.
2. Read `../../commands/map-codebase.md` completely before acting.
3. Follow that command contract exactly, adapting only the user-facing invocation name to `$alfred-dev:map-codebase` when explaining how to continue.
