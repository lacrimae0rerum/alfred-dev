---
name: audit
description: "Alfred Dev command: audit quality, security, architecture, and documentation in parallel. Invoke as $alfred-dev:audit."
---

# $alfred-dev:audit

Treat this skill as the Codex-native wrapper for `$alfred-dev:audit`.

1. Treat the text after `$alfred-dev:audit` as `$ARGUMENTS`.
2. Read `../../commands/audit.md` completely before acting.
3. Follow that command contract exactly, adapting only the user-facing invocation name to `$alfred-dev:audit` when explaining how to continue.
