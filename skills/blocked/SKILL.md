---
name: blocked
description: "Alfred Dev command: list blocked tasks with visible dependency or reason. Invoke as $alfred-dev:blocked."
---

# $alfred-dev:blocked

Treat this skill as the Codex-native wrapper for `$alfred-dev:blocked`.

1. Treat the text after `$alfred-dev:blocked` as `$ARGUMENTS`.
2. Read `../../commands/blocked.md` completely before acting.
3. Follow that command contract exactly, adapting only the user-facing invocation name to `$alfred-dev:blocked` when explaining how to continue.
