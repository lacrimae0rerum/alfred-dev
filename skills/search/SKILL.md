---
name: search
description: "Alfred Dev command: search SonIA artifacts and persistent SQLite memory. Invoke as $alfred-dev:search."
---

# $alfred-dev:search

Treat this skill as the Codex-native wrapper for `$alfred-dev:search`.

1. Treat the text after `$alfred-dev:search` as `$ARGUMENTS`.
2. Read `../../commands/search.md` completely before acting.
3. Follow that command contract exactly, adapting only the user-facing invocation name to `$alfred-dev:search` when explaining how to continue.
