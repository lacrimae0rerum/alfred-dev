---
name: sync-github
description: "Alfred Dev command: sync the local SonIA board with GitHub Issues. Invoke as $alfred-dev:sync-github."
---

# $alfred-dev:sync-github

Treat this skill as the Codex-native wrapper for `$alfred-dev:sync-github`.

1. Treat the text after `$alfred-dev:sync-github` as `$ARGUMENTS`.
2. Read `../../commands/sync-github.md` completely before acting.
3. Follow that command contract exactly, adapting only the user-facing invocation name to `$alfred-dev:sync-github` when explaining how to continue.
