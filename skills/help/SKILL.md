---
name: help
description: "Alfred Dev command: show command, agent, and flow reference. Invoke as $alfred-dev:help."
---

# $alfred-dev:help

Treat this skill as the Codex-native wrapper for `$alfred-dev:help`.

1. Treat the text after `$alfred-dev:help` as `$ARGUMENTS`.
2. Read `../../commands/help.md` completely before acting.
3. Follow that command contract exactly, adapting only the user-facing invocation name to `$alfred-dev:help` when explaining how to continue.
