---
name: lucius
description: "Alfred Dev command: request an external technical second opinion through Codex CLI. Invoke as $alfred-dev:lucius."
---

# $alfred-dev:lucius

Treat this skill as the Codex-native wrapper for `$alfred-dev:lucius`.

1. Treat the text after `$alfred-dev:lucius` as `$ARGUMENTS`.
2. Read `../../commands/lucius.md` completely before acting.
3. Follow that command contract exactly, adapting only the user-facing invocation name to `$alfred-dev:lucius` when explaining how to continue.
