---
name: ship
description: "Alfred Dev command: release flow with final audit, changelog, semantic versioning, packaging, and deployment gates. Invoke as $alfred-dev:ship."
---

# $alfred-dev:ship

Treat this skill as the Codex-native wrapper for `$alfred-dev:ship`.

1. Treat the text after `$alfred-dev:ship` as `$ARGUMENTS`.
2. Read `../../commands/ship.md` completely before acting.
3. Follow that command contract exactly, adapting only the user-facing invocation name to `$alfred-dev:ship` when explaining how to continue.
