---
name: spike
description: "Alfred Dev command: technical investigation without implementation commitment. Invoke as $alfred-dev:spike."
---

# $alfred-dev:spike

Treat this skill as the Codex-native wrapper for `$alfred-dev:spike`.

1. Treat the text after `$alfred-dev:spike` as `$ARGUMENTS`.
2. Read `../../commands/spike.md` completely before acting.
3. Follow that command contract exactly, adapting only the user-facing invocation name to `$alfred-dev:spike` when explaining how to continue.
