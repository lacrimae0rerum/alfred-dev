---
name: memory-ui
description: "Alfred Dev command: open the local memory UI for SQLite decisions, timeline, graph, commits, and search. Invoke as $alfred-dev:memory-ui."
---

# $alfred-dev:memory-ui

Treat this skill as the Codex-native wrapper for `$alfred-dev:memory-ui`.

1. Treat the text after `$alfred-dev:memory-ui` as `$ARGUMENTS`.
2. Read `../../commands/memory-ui.md` completely before acting.
3. Follow that command contract exactly, adapting only the user-facing invocation name to `$alfred-dev:memory-ui` when explaining how to continue.
