---
name: quick
description: "Alfred Dev command: small scoped change with focused implementation, tests, validation, and security. Invoke as $alfred-dev:quick."
---

# $alfred-dev:quick

Treat this skill as the Codex-native wrapper for `$alfred-dev:quick`.

1. Treat the text after `$alfred-dev:quick` as `$ARGUMENTS`.
2. Read `../../commands/quick.md` completely before acting.
3. Follow that command contract exactly, adapting only the user-facing invocation name to `$alfred-dev:quick` when explaining how to continue.
