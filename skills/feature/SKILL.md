---
name: feature
description: "Alfred Dev command: full feature flow through product, visual style when needed, architecture, development, QA, docs, and delivery. Invoke as $alfred-dev:feature."
---

# $alfred-dev:feature

Treat this skill as the Codex-native wrapper for `$alfred-dev:feature`.

1. Treat the text after `$alfred-dev:feature` as `$ARGUMENTS`.
2. Read `../../commands/feature.md` completely before acting.
3. Follow that command contract exactly, adapting only the user-facing invocation name to `$alfred-dev:feature` when explaining how to continue.
